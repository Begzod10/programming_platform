"""Russian translation for Capstone 6: Accessibility va Brauzer API, lesson order=6 (L7)."""
from __future__ import annotations
import asyncio, sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from sqlalchemy import select  # noqa: E402
from app.db.database import AsyncSessionLocal  # noqa: E402
from app.db import base as _base  # noqa: E402,F401
from app.models.lesson import Lesson  # noqa: E402
from app.models.exercise import Exercise  # noqa: E402
from write_ru_translations import translate_lesson, translate_exercises  # noqa: E402

LESSON_ID = 812

TITLE_RU = "7-Финальная полировка и деплой (завершение CAPSTONE)"

TEXT_RU = """\
<h2>Этап 7 (завершение CAPSTONE): деплой и ошибка "Lighthouse дал 100 баллов"</h2>

<pre class="mermaid">
flowchart LR
    DEPLOY["AccessBoard деплоится"] --> AUDIT["Запускается автоматический аудит Lighthouse/axe"]
    AUDIT --> SCORE["Результат: 100/100 - 'Accessibility: A'"]
    SCORE --> SHIP["Команда: 'Всё в порядке', ПРОПУСКАЕТ ручное тестирование"]
    SHIP --> REAL["При тестировании с реальным screen reader: ошибки из уроков 3 и 5 МОГУТ ВСЁ ЕЩЁ существовать"]
</pre>

<p>Это — финальный этап AccessBoard. И здесь раскрывается самое <strong>окончательное</strong> проявление идеи, которую вы видели на протяжении всего capstone: автоматические инструменты проверки доступности (Lighthouse, axe) <strong>полезны</strong>, но они <strong>не могут проверить всё</strong> — и "100/100 баллов", как и "зелёный CI" в Capstone 5, может дать <strong>ложное спокойствие</strong>.</p>

<h3>🏆 Победа за 5 минут</h3>

<h4>БЛОК 1 — автоматический аудит: Lighthouse/axe</h4>
<pre><code># Terminal:
npx lighthouse https://accessboard.example.com \\
  --only-categories=accessibility --output=json --output-path=report.json

# Результат (пример):
# Accessibility: 100/100</code></pre>

<h4>БЛОК 2 — что МОГУТ проверить автоматические инструменты</h4>
<pre><code># Проверки, НА КОТОРЫЕ СПОСОБЕН Lighthouse/axe (структурные/синтаксические):
# - Есть ли атрибут alt у <img>?
# - Есть ли <label> (или aria-label) для каждого <input>?
# - Вычисляется ли ЧИСЛЕННО коэффициент контраста текста/фона (4.5:1)?
# - Написаны ли атрибуты ARIA с ПРАВИЛЬНЫМ синтаксисом?
# - Указан ли язык HTML (атрибут lang)?
#
# Всё это РЕАЛЬНО и ВАЖНО - но это отвечает только на вопрос "верна ли
# СТРУКТУРА", а не на вопрос "верен ли ОПЫТ".</code></pre>

<h4>БЛОК 3 — РУЧНОЕ тестирование: то, что автоматический инструмент НИКОГДА не проверит</h4>
<pre><code># Проверяется вручную, С РЕАЛЬНОЙ клавиатурой и screen reader:
# - Идёт ли порядок Tab ЛОГИЧНО (соответствует визуальному порядку)?
# - При прослушивании через screen reader понятен ли СМЫСЛ правильно?
# - Как на уроке 3: ДЕЙСТВИТЕЛЬНО ли сохраняется фокус при обновлении WebSocket?
# - Как на уроке 5: можно ли ДЕЙСТВИТЕЛЬНО выполнить все задачи
#   (включая прикрепление файла) с клавиатуры?
#
# ЭТО Lighthouse/axe НИКОГДА не может проверить автоматически - это
# обнаруживается только при симуляции РЕАЛЬНОГО пользовательского опыта.</code></pre>

<h3>🐛 Намеренная ошибка — доверие только автоматическому баллу, пропуск ручного тестирования</h3>
<pre><code># .github/workflows/deploy.yml
jobs:
  a11y-check:
    steps:
      - run: npx lighthouse $URL --only-categories=accessibility --output=json > report.json
      - run: node check-score.js report.json   # 90+ считается успехом
  deploy:
    needs: a11y-check
    # ❌ Это ЕДИНСТВЕННАЯ проверка - ручное тестирование с screen
    #    reader/клавиатурой НИКОГДА не проводилось!

# Команда ВЕРИТ: "Lighthouse дал 100/100, значит AccessBoard полностью
# доступен" и деплоит.
#
# НО: Lighthouse НИКОГДА не может обнаружить следующее:
# - Ошибка потери фокуса с 3-го урока МОЖЕТ ВСЁ ЕЩЁ существовать
#   (потому что автоматический инструмент не способен "попробовать"
#   поведение фокуса во время обновления WebSocket)
# - Ошибка "только drag-and-drop" с 5-го урока ТОЖЕ может всё ещё
#   существовать (потому что сама dropzone может иметь правильные
#   атрибуты ARIA, но РЕАЛЬНО не работать с клавиатуры - Lighthouse
#   этого "не пробует", он проверяет только статический код)
#
# ❌ ДАЖЕ с баллом 100/100 AccessBoard МОЖЕТ ВСЁ ЕЩЁ частично не
#    работать для реального пользователя клавиатуры/screen reader -
#    это НИКОГДА не обнаруживается автоматическим аудитом.</code></pre>

<p><strong>Результат:</strong> автоматические инструменты проверки доступности (Lighthouse, axe) способны обнаружить <strong>примерно 30-40%</strong> реальных проблем WCAG (это - широко признанный, документированный факт) — оставшиеся <strong>60-70%</strong> обнаруживаются только <strong>реальным, ручным</strong> тестированием (выполнение каждой задачи с клавиатуры, прослушивание через screen reader). "Балл 100/100" — это сигнал <strong>структурной корректности</strong>, а не сигнал <strong>корректности опыта</strong>. Как и "зелёный CI" в Capstone 5, это показывает опасность доверия <strong>автоматизированному прокси</strong> вместо <strong>реального пользовательского опыта</strong>.</p>

<h3>Теперь объясним</h3>

<h4>1. Что на самом деле проверяют инструменты вроде Lighthouse/axe?</h4>
<p>Эти инструменты <strong>статически анализируют</strong> структуру HTML/CSS/ARIA — например "есть ли <code>alt</code> у этого <code>&lt;img&gt;</code>", "эта комбинация цветов выше 4.5:1?". Они <strong>читают</strong> код, но <strong>не пробуют использовать его</strong> как реальный пользователь.</p>

<h4>2. Почему они не могут проверить управление фокусом (урок 3) или полноту клавиатуры (урок 5)?</h4>
<p>Эти проблемы связаны с <strong>динамическим поведением</strong> — куда уходит фокус при получении обновления WebSocket, или можно ли реально прикрепить файл с клавиатуры. Автоматический инструмент анализирует страницу в <strong>статическом состоянии</strong> - он не "пробует" отправить сообщение WebSocket и затем проверить, где находится фокус.</p>

<h4>3. Что означает цифра "30-40%"?</h4>
<p>Это - широко признанный в сообществе доступности, подтверждённый различными исследованиями приблизительный показатель: автоматические инструменты способны <strong>программно</strong> проверить лишь часть критериев WCAG. Остальное - такие вещи, как <strong>смысл</strong>, <strong>логический порядок</strong>, <strong>реальное удобство использования</strong> - требует человеческого суждения и тестирования.</p>

<h4>4. Каков правильный подход?</h4>
<p>Использовать автоматические инструменты как <strong>первый уровень</strong> (быстро, дёшево, ловит много простых ошибок), но не использовать их как <strong>единственный</strong> gate. Перед деплоем обязательно нужно провести минимум одно <strong>реальное</strong> тестирование только клавиатурой и (по возможности) с реальным screen reader.</p>

<h4>5. Какое место занимает этот урок в capstone?</h4>
<p>Это - <strong>финальный</strong> урок 7-этапного capstone: подобно тому, как в Capstone 5 "зелёный тест/высокий coverage/успешный CI" не гарантировали реальную корректность, здесь "высокий автоматический балл доступности" тоже не гарантирует реальный, <strong>инклюзивный</strong> опыт. Оба показывают одну глубокую истину: <strong>измерительный инструмент - это не сама истина, а его ограниченный прокси.</strong></p>

<h3>📌 После этого урока вы знаете</h3>
<ul>
<li>✅ Автоматические инструменты доступности (Lighthouse, axe) обнаруживают лишь примерно 30-40% реальных проблем</li>
<li>✅ Они проверяют структурные/синтаксические вещи, а не динамическое поведение или смысл</li>
<li>✅ "Балл 100/100" - сигнал структурной корректности, а не гарантия реального опыта</li>
<li>✅ Проблемы вроде управления фокусом и полноты клавиатуры обнаруживаются только ручным тестированием</li>
<li>✅ Правильный подход: автоматические инструменты + обязательное ручное тестирование клавиатурой/screen reader, вместе</li>
</ul>

<h3>🎉 Поздравляем!</h3>
<p>Вы построили AccessBoard с нуля - с документа плана "клавиатура прежде всего" на этапе 1, через семантический HTML и ARIA, навигацию с клавиатуры и WebSocket, контраст цветов и IndexedDB, доступность форм и File API, Service Worker и PWA, и, наконец, до <strong>правильной, надёжной проверки доступности</strong>. За этот capstone вы объединили знания, полученные отдельно на курсах Veb Accessibility и JavaScript: Brauzer API va Web, в <strong>одном реальном проекте</strong> - и, что самое важное, в отличие от пяти других capstone-проектов, вы увидели не обман TypeScript или тестов, а то, насколько <strong>относительно само понятие "работает"</strong>, в семи разных проявлениях: <strong>то, что интерфейс работает для вас - мышью, вашими глазами, для автоматического инструмента, - не гарантирует, что он работает для КАЖДОГО, кто им пользуется.</strong></p>
"""

CODE_RU = """\
// ════════════════════════════════════════════════════════════════════
// ЭТАП 7 (ЗАВЕРШЕНИЕ CAPSTONE): Деплой и ошибка автоматического аудита
// ════════════════════════════════════════════════════════════════════

// ─────────────────────────────────────────────────────────────────────
// 1) .github/workflows/deploy.yml - ПРАВИЛЬНО: автоматика + обязательный ручной чеклист (в комментарии)
// ─────────────────────────────────────────────────────────────────────

// jobs:
//   a11y-automated-check:
//     steps:
//       - run: npx lighthouse $URL --only-categories=accessibility --output=json > report.json
//       - run: node check-score.js report.json
//   deploy:
//     needs: a11y-automated-check
//     steps:
//       - run: ./deploy.sh
//
// # В README.md ОБЯЗАТЕЛЬНЫЙ список ручной проверки (ПЕРЕД деплоем,
// # выполняется человеком):
// # - [ ] Все задачи выполнены только Tab/Shift+Tab/Enter
// # - [ ] Доску можно понять "на слух" с VoiceOver/NVDA
// # - [ ] Проверено сохранение фокуса при обновлении WebSocket

// ─────────────────────────────────────────────────────────────────────
// 2) check-score.js - проверка автоматического балла (это ТОЛЬКО первый уровень)
// ─────────────────────────────────────────────────────────────────────

const fs = require('fs');
const report = JSON.parse(fs.readFileSync(process.argv[2]));
const score = report.categories.accessibility.score * 100;

console.log(`Балл доступности Lighthouse: ${score}/100`);
if (score < 90) {
  console.error('Балл ниже 90 - деплой остановлен.');
  process.exit(1);
}
// ❗ Это ТОЛЬКО первый фильтр - не заменяет ручное тестирование!

// ─────────────────────────────────────────────────────────────────────
// 3) Намеренная ошибка - ТОЛЬКО автоматический балл, без ручного тестирования (в комментарии)
// ─────────────────────────────────────────────────────────────────────

// jobs:
//   a11y-check:
//     steps:
//       - run: npx lighthouse $URL --only-categories=accessibility --output=json > report.json
//       - run: node check-score.js report.json
//   deploy:
//     needs: a11y-check
//     # Больше НИКАКОЙ проверки нет!
//
// Даже с баллом 100/100, ошибка фокуса с 3-го урока и ошибка
// "только-drag-and-drop" с 5-го урока МОГУТ ВСЁ ЕЩЁ быть в
// production - Lighthouse их НИКОГДА не обнаружит.
"""

EX = {
    4664: {
        "title": "Что на самом деле проверяют инструменты вроде Lighthouse/axe?",
        "description": "Какой тип проверок в основном выполняют автоматические инструменты доступности (Lighthouse, axe)?",
        "hint": "Эти инструменты 'читают' код, но 'пробуют ли' его использовать?",
        "explanation": "Автоматические инструменты статически анализируют структуру HTML/CSS/ARIA - они читают код, но не пробуют использовать его как реальный пользователь, поэтому не могут обнаружить динамическое поведение.",
    },
    4665: {
        "title": "Почему Lighthouse не может обнаружить ошибку фокуса с 3-го урока?",
        "description": "Почему автоматический инструмент вроде Lighthouse не может обнаружить потерю фокуса при обновлении WebSocket (урок 3)?",
        "hint": "Когда возникает ошибка фокуса - при открытии страницы, или позже, когда происходит какое-то событие?",
        "explanation": "Эта проблема связана с динамическим поведением - автоматический инструмент анализирует страницу в статическом состоянии, у него нет возможности отправить сообщение WebSocket и затем проверить, где находится фокус.",
    },
    4666: {
        "title": "Расположите, как ошибка попадает в production даже с баллом 100/100",
        "description": "Расположите процесс того, как доверие только автоматическому баллу и пропуск ручного тестирования выпускает сломанную доступность в production.",
        "hint": "",
        "explanation": "",
    },
    4667: {
        "title": "Какой примерный процент реальных проблем WCAG обнаруживают автоматические инструменты?",
        "description": "По широко признанному в сообществе доступности приблизительному показателю, какой примерно процент реальных проблем WCAG автоматические инструменты проверки могут обнаружить программно? (напишите диапазон в процентах, например: X-Y%)",
        "hint": "В тексте этого урока указана именно эта цифра.",
        "expected_answer": "30-40%",
    },
    4668: {
        "title": "Почему 'высокий автоматический балл' и 'зелёный CI' (Capstone 5) имеют одну и ту же глубокую ошибку?",
        "description": (
            "Какую общую глубокую идею имеет ошибка 'высокого балла "
            "Lighthouse' в этом уроке с ошибкой 'зелёный CI, но тесты "
            "на самом деле провалились' в Capstone 5? Объясните своими "
            "словами."
        ),
        "hint": "Что общего есть в обоих случаях - числовой/визуальный сигнал, и что он НЕ ГАРАНТИРУЕТ?",
        "expected_answer": "Оба показывают одну и ту же глубокую истину: измерительный инструмент (автоматический балл доступности, или сигнал 'успешно' от CI) - это не сама истина, а его ОГРАНИЧЕННЫЙ, неполный ПРОКСИ (заместитель). В обоих случаях есть риск того, что команда доверится удобному, быстрому числовому сигналу (высокий балл, зелёная галочка) и пропустит более медленную, но БОЛЕЕ ТОЧНУЮ проверку (ручное тестирование доступности, или проверку того, что тесты действительно прошли). В обоих случаях опасность в том, что метрика может быть высокой, даже если реальная система (код, или пользовательский опыт) всё ещё сломана.",
    },
}


async def _run():
    async with AsyncSessionLocal() as db:
        lesson = (await db.execute(select(Lesson).where(Lesson.id == LESSON_ID))).scalar_one()
        ex_rows = (
            await db.execute(select(Exercise).where(Exercise.id.in_(EX.keys())))
        ).scalars().all()

        section_map = {"Текст": "Текст", "Код": "Код", "Упражнения": "Упражнения"}
        section_map[lesson.text_content] = TEXT_RU
        section_map[lesson.code_content] = CODE_RU
        TASK_TITLE_RU = "AccessBoard — завершение CAPSTONE: проект с полной проверкой доступности"
        TASK_DESCRIPTION_RU = (
            "Задеплойте AccessBoard на реальный хостинг и добавьте в CI "
            "автоматический аудит доступности (Lighthouse или axe). "
            "Кроме того, ПЕРЕД деплоем проведите обязательное РУЧНОЕ "
            "тестирование: выполните все основные задачи только с "
            "клавиатуры и (по возможности) протестируйте с screen reader."
        )
        TASK_REQUIREMENTS_RU = (
            "• В CI запускается автоматический аудит доступности через Lighthouse или axe (требуется балл 90+)\n"
            "• Ручная проверка: все основные задачи (открытие карточки, перемещение, прикрепление файла) выполнены только Tab/Shift+Tab/Enter\n"
            "• Ручная проверка: РЕАЛЬНО подтверждено сохранение фокуса при обновлении WebSocket\n"
            "• Flask/Node backend работает на реальном хостинге\n"
            "• README.md: живая ссылка, результаты автоматического + ручного тестирования, чеклист завершения 7/7 этапов\n"
            "• Для отправки требуется ТОЛЬКО GitHub URL репозитория"
        )
        TASK_TECHNOLOGIES_RU = "Node.js, Express, Lighthouse, axe DevTools, Render/Railway"
        if lesson.task_title:
            section_map[lesson.task_title] = TASK_TITLE_RU
        if lesson.task_description:
            section_map[lesson.task_description] = TASK_DESCRIPTION_RU
        for ex in ex_rows:
            for field_name, translated in EX[ex.id].items():
                source = getattr(ex, field_name)
                if source:
                    section_map[source] = translated

        await translate_lesson(
            db, LESSON_ID,
            flat_fields={
                "title": TITLE_RU,
                "text_content": TEXT_RU,
                "task_title": TASK_TITLE_RU,
                "task_description": TASK_DESCRIPTION_RU,
                "task_requirements": TASK_REQUIREMENTS_RU,
                "task_technologies": TASK_TECHNOLOGIES_RU,
            },
            section_translations=section_map,
        )
        await translate_exercises(db, EX)
        await db.commit()
        print(f"Lesson {LESSON_ID}: wrote {len(section_map)} section strings")


if __name__ == "__main__":
    asyncio.run(_run())
