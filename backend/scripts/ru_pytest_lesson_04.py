"""Russian translation for Python: Testlash, lesson order=3 (L4)."""
from __future__ import annotations
import asyncio, sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from sqlalchemy import select  # noqa: E402
from app.db.database import AsyncSessionLocal  # noqa: E402
from app.db import base as _base  # noqa: E402,F401
from app.models.lesson import Lesson  # noqa: E402
from app.models.exercise import Exercise  # noqa: E402
from write_ru_translations import translate_lesson, translate_exercises  # noqa: E402

LESSON_ID = 640

TITLE_RU = "4-TDD (Test Driven Development)"

TEXT_RU = """\
<h2>TDD (Test Driven Development) — сначала тест, потом код</h2>

<pre class="mermaid">
flowchart LR
    RED["🔴 RED — пишется неудачный тест"] --> GREEN["🟢 GREEN — минимальный код, проходящий тест"]
    GREEN --> REFACTOR["🔵 REFACTOR — улучшение кода, тесты всё ещё проходят"]
    REFACTOR --> RED
</pre>

<p>До сих пор мы сначала писали код, потом добавляли тест. TDD (Test Driven Development) делает это <strong>наоборот</strong>: сначала пишется неудачный тест, затем — простейший код, чтобы его пройти, потом код улучшается. Это цикл Red-Green-Refactor.</p>

<h3>🏆 Победа за 5 минут</h3>

<h4>БЛОК 1 — 🔴 RED: сначала неудачный тест</h4>
<pre><code># test_baholash.py — функция ЕЩЁ не существует!
from baholash import harfli_baho

def test_harfli_baho_a():
    assert harfli_baho(95) == "A"</code></pre>

<pre><code># Терминал:
pytest
# ModuleNotFoundError: No module named 'baholash'
# ❗ Это — ожидаемое состояние! Код ещё не написан.</code></pre>

<h4>БЛОК 2 — 🟢 GREEN: ПРОСТЕЙШИЙ код, проходящий тест</h4>
<pre><code># baholash.py
def harfli_baho(ball):
    return "A"  # ❗ пока просто чтобы пройти тест</code></pre>

<pre><code># Терминал:
pytest
# ================ 1 passed in 0.01s ================</code></pre>

<p>Удивительно, но это <strong>правильный TDD</strong>: пока есть только один тест, и он прошёл. Следующий шаг — добавить ещё один тест и приблизить код к реальной логике.</p>

<h4>БЛОК 3 — снова RED → GREEN, постепенное дополнение кода</h4>
<pre><code># Добавлен ещё один тест:
def test_harfli_baho_b():
    assert harfli_baho(82) == "B"

def test_harfli_baho_f():
    assert harfli_baho(40) == "F"</code></pre>

<pre><code># Терминал: pytest → 2 FAILED (потому что всё ещё возвращает "A")

# Теперь дополняем код реальной логикой — 🟢 GREEN:
def harfli_baho(ball):
    if ball >= 90:
        return "A"
    elif ball >= 80:
        return "B"
    elif ball >= 70:
        return "C"
    else:
        return "F"</code></pre>

<pre><code># Терминал:
pytest
# ================ 3 passed in 0.01s ================</code></pre>

<h3>🐛 Намеренная ошибка — остановиться на "GREEN", подгоняя только под один тест</h3>
<pre><code># ❌ Смотря только на 1-й тест, "подгоняем" функцию под него:
def harfli_baho(ball):
    if ball == 95:  # ❌ работает только для этого конкретного значения!
        return "A"
    return "F"  # для всего остального неверно</code></pre>

<p><strong>Результат:</strong> <code>test_harfli_baho_a()</code> (со значением 95) проходит, но функция на самом деле <strong>не реализует общую логику</strong> — она "подогнана" только под одно конкретное тестовое значение. Если больше не написать ни одного теста, эта ошибка никогда не будет обнаружена. Именно в этом сила TDD: <strong>добавляя больше тестов</strong>, постепенно доказывается, что функция действительно работает для общих случаев. Один тест ничего не гарантирует; набор тестов (с разными граничными случаями) даёт реальную уверенность.</p>

<h3>Теперь объясним</h3>

<h4>1. Почему сначала тест, потом код?</h4>
<p>Это заставляет заранее определить <strong>чёткое требование</strong>: что должна делать функция, какой именно результат ожидается. Если тест добавляется после написания кода, часто тест "подгоняется под код", хотя должно быть наоборот.</p>

<h4>2. Почему важен этап RED?</h4>
<p>Тест сначала обязательно должен <strong>провалиться</strong> — это подтверждает, что сам тест работает правильно. Если написанный тест проходит даже без кода — значит тест написан неверно (ничего не проверяет).</p>

<h4>3. Почему на этапе GREEN достаточно "простейшего кода"?</h4>
<p>Цель — быстро прийти к рабочему состоянию. Следующие тесты естественным образом заставят сделать код более полным и общим. Возврат "A" в одном тесте — временное решение, оно обязательно расширится следующими тестами.</p>

<h4>4. Что делает этап REFACTOR?</h4>
<p>После того как все тесты проходят, можно <strong>очистить код, упорядочить, убрать дублирование</strong> — тесты выполняют роль "страховочной сетки": если во время рефакторинга что-то сломается, тесты сразу покажут ошибку.</p>

<h4>5. Почему одного теста недостаточно?</h4>
<p>Один успешный тест показывает, что функция верна <strong>только для этого конкретного случая</strong>, а не то, что верна общая логика. Написание отдельных тестов для граничных случаев (например 90, 89, 0, отрицательное число) доказывает, что функция действительно надёжна.</p>

<h3>📌 После этого урока вы знаете</h3>
<ul>
<li>✅ TDD — цикл Red (неудачный тест) → Green (минимальный код) → Refactor (улучшение)</li>
<li>✅ Тест пишется сначала, код — потом — это заставляет чётко определить требование</li>
<li>✅ На этапе RED тест обязательно должен быть FAILED — это подтверждает, что тест работает</li>
<li>✅ На этапе GREEN достаточно минимального кода, только чтобы пройти тесты</li>
<li>✅ Код, "подогнанный" под один тест, опасен — только добавление новых тестов проверяет общую логику</li>
</ul>
"""

CODE_RU = """\
# ════════════════════════════════════════════════════════════════════
# УРОК 4: TDD (Test Driven Development)
# ════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────
# 🔴 RED — test_baholash.py (функция ещё не существует)
# ─────────────────────────────────────────────────────────────────────

# from baholash import harfli_baho
#
# def test_harfli_baho_a():
#     assert harfli_baho(95) == "A"
#
# Терминал: pytest -> ModuleNotFoundError (ожидаемое состояние!)


# ─────────────────────────────────────────────────────────────────────
# 🟢 GREEN — baholash.py (простейший код, только для 1-го теста)
# ─────────────────────────────────────────────────────────────────────

def harfli_baho_v1(ball):
    return "A"  # временно — пока только один тест


# ─────────────────────────────────────────────────────────────────────
# Добавлены новые тесты -> снова RED -> расширяем код -> GREEN
# ─────────────────────────────────────────────────────────────────────

def harfli_baho(ball):
    if ball >= 90:
        return "A"
    elif ball >= 80:
        return "B"
    elif ball >= 70:
        return "C"
    else:
        return "F"


# test_baholash.py — полный набор тестов
def test_harfli_baho_a():
    assert harfli_baho(95) == "A"

def test_harfli_baho_b():
    assert harfli_baho(82) == "B"

def test_harfli_baho_c():
    assert harfli_baho(75) == "C"

def test_harfli_baho_f():
    assert harfli_baho(40) == "F"

def test_harfli_baho_chegara():
    assert harfli_baho(90) == "A"  # граничное значение — 90 это именно A
    assert harfli_baho(89) == "B"  # на единицу меньше границы — B


# ─────────────────────────────────────────────────────────────────────
# Намеренная ошибка — код, подогнанный под один тест (в комментарии)
# ─────────────────────────────────────────────────────────────────────

# def harfli_baho_xato(ball):
#     if ball == 95:  # ❌ работает только для этого конкретного значения!
#         return "A"
#     return "F"  # для всего остального неверно
"""

EX = {
    3775: {
        "title": "В каком порядке выполняется цикл TDD?",
        "description": "В каком порядке последовательно выполняются три этапа TDD (Test Driven Development)?",
        "hint": "Сначала неудачный тест, затем минимальный код, затем улучшение.",
        "explanation": "Цикл TDD: RED (пишется неудачный тест) -> GREEN (пишется минимальный код, проходящий тест) -> REFACTOR (код улучшается, тесты всё ещё проходят).",
    },
    3776: {
        "title": "Какой результат должен давать тест на этапе RED?",
        "description": "Что должно происходить при запуске теста, написанного на этапе RED в TDD?",
        "hint": "На этом этапе код ещё не написан или не удовлетворяет тесту.",
        "explanation": "На этапе RED тест обязательно должен быть FAILED — это подтверждает, что сам тест работает правильно и действительно что-то проверяет.",
    },
    3777: {
        "title": "Расположите последовательность практики TDD в правильном порядке",
        "description": "Упорядочите шаги создания функции harfli_baho методом TDD.",
        "hint": "Сначала тест, затем неудача, затем минимальный код, затем расширение.",
    },
    3778: {
        "title": "Почему опасен код, подогнанный под один тест?",
        "description": (
            "Если разработчик напишет функцию harfli_baho(ball) только как "
            "'if ball == 95: return \"A\"', без общей логики для остальных "
            "случаев, но тест пройдёт — почему это опасно? Как TDD решает "
            "эту проблему? Объясните своими словами."
        ),
        "expected_answer": "Если функция подогнана только под одно конкретное тестовое значение (например 95), она может пройти тест, но это не гарантирует, что функция правильно реализует общую логику — например, для значений вроде 82 или 40 она может дать совершенно неверный результат. Если больше не написать ни одного теста, эта ошибка никогда не будет обнаружена. TDD решает эту проблему добавлением новых тестов: каждый новый тест постепенно заставляет функцию приближаться к настоящей, общей логике, не позволяя ограничиться подгонкой под один случай.",
        "hint": "Один тест проверяет только один случай. Как доказать общую логику?",
    },
}


async def _run():
    async with AsyncSessionLocal() as db:
        lesson = (await db.execute(select(Lesson).where(Lesson.id == LESSON_ID))).scalar_one()
        ex_rows = (
            await db.execute(select(Exercise).where(Exercise.id.in_(EX.keys())))
        ).scalars().all()

        section_map = {"Текст": "Текст", "Код": "Код", "Упражнения": "Упражнения"}
        section_map[lesson.text_content] = TEXT_RU
        section_map[lesson.code_content] = CODE_RU
        for ex in ex_rows:
            for field_name, translated in EX[ex.id].items():
                source = getattr(ex, field_name)
                if source:
                    section_map[source] = translated

        await translate_lesson(
            db, LESSON_ID,
            flat_fields={"title": TITLE_RU, "text_content": TEXT_RU},
            section_translations=section_map,
        )
        await translate_exercises(db, EX)
        await db.commit()
        print(f"Lesson {LESSON_ID}: wrote {len(section_map)} section strings")


if __name__ == "__main__":
    asyncio.run(_run())
